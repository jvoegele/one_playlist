# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Temporary files, for example, from tests.
/tmp/

# Ignore package tarball (built via "mix hex.build").
one_playlist-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/


# Dialyzer PLTs — large, machine-specific, and rebuildable.
/priv/plts/*.plt
/priv/plts/*.plt.hash

# Local development overrides — real provider credentials, never committed.
# Copy config/dev_local.example.exs to config/dev_local.exs and fill it in.
/config/dev_local.exs

# The local Navidrome instance: generated audio and its database. The generator
# and the compose file are committed; what they produce is not.
/dev/navidrome/music/
/dev/navidrome/data/

# Roon exports of real listening history. Personal, and this repository is
# public — a "Loved Tracks" export is a couple of thousand rows of what somebody
# actually listens to.
#
# Not ignored before, which was easy to get wrong because the four entries
# around it made `dev/` *look* ignored. It is not: only these specific paths are.
# They were committed by a `git add -A` and removed from the unpushed history
# again with `git filter-branch`.
#
# The corpora under `dev/corpus/` are a deliberate exception and stay tracked —
# they are sampled and derived rather than a verbatim export, and the engine
# cannot be measured without them.
/dev/playlists/

# Experiment scratch: which playlist and which transfer a dev script is pointed at.
dev/experiments/PLAYLIST
dev/experiments/TRANSFER
